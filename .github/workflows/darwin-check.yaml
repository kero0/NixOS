# This is currently disabled because github actions doesn't have an aarch4-darwin runner
# name: "Darwin check"
# on:
#   workflow_dispatch:
#   pull_request:
#   push:
# jobs:
#   darwin-check:
#     runs-on: macos-latest
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4
#       - name: Install Nix
#         uses: DeterminateSystems/nix-installer-action@v1
#       - name: Cache nix
#         uses: DeterminateSystems/magic-nix-cache-action@main
#       - run: nix build .#darwinConfigurations.Kirolss-MacBook-Air.system
